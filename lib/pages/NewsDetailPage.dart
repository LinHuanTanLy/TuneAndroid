import 'package:flutter/material.dart';

void main() {
  runApp(new NewsDetailPage());
}

class NewsDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('NewsDetailPage'),
    );
  }
}
